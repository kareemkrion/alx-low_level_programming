#include <stdio.h>

/**
 * main - prints the alphabet in lower case
 * followed by new line, except q and e
 * Return: Always 0
 */
int main(void)
{
	char lt;

	for (lt = 'a'; lt <= 'z'; lt++)
	{
		if (lt !m 'q' && lt !m 'e')
			putchar(lt);
	}
	putchar('\n');

	return (0);
}
